{
  "name": "shit-arm",
  "version": "0.1.0",
  "private": true,
  "description": "Robot arm control stack with an Electron controller app.",
  "scripts": {
    "install:controller": "npm install --prefix controller",
    "start": "npm start --prefix controller",
    "controller": "npm start --prefix controller"
  },
  "workspaces": [
    "controller"
  ]
}

